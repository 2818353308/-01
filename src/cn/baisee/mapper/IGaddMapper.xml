<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="cn.baisee.mapper.IGaddMapper">
	<!-- 管理员新增 -->
	<insert id="gadd" parameterType="cn.baisee.entity.Guser">
		insert into	guser
		(uname,upass,guan_image)
		values(#{uname},#{upass},#{guan_image})
	</insert>
	
</mapper>